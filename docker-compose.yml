services:
  gmail-mcp-community:
    build: .
    container_name: gmail-mcp-community
    restart: unless-stopped
    ports:
      - "127.0.0.1:3034:3000"
    mem_limit: 512m
    mem_reservation: 256m
